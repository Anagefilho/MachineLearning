O código apenas usa a biblioteca numpy, para ser usada é preciso ter a importado: import numpy as np.
O código é em python 3.7, foi feito em windows no Spyder, para ser usado no Linux
creio que só compilando pelo terminal irá funcionar.

O código todo esta no arquivo main.py